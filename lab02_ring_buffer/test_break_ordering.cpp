/**
 * @file test_break_ordering.cpp
 * @brief Deliberately broken memory ordering experiment.
 *
 * Change release to relaxed on available_ in push().
 * Run stress test 100 times. Document results in RESULTS.md.
 */
#include <cstdio>

int main() {
    std::printf("Lab 02 - Break Ordering: IMPLEMENT AFTER STRESS TEST PASSES\n");
    return 0;
}
